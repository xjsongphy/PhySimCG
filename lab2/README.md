# Lab 2: Fluid Simulation

## Run

From repository root:

```bash
uv run lab2
```

Run a specific demo:

```bash
uv run lab2 --demo basic
uv run lab2 --demo b2
uv run lab2 --demo b3
uv run lab2 --demo b4
uv run lab2 --demo b5
```

Enable debug mode:

```bash
uv run lab2 --demo basic --debug
```
